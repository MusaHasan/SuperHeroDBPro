@*<EditForm Model="Hero" OnSubmit="SubmitCall">
    <div class="form-group">
        <label>First Name</label>
        <InputText @bind-Value="Hero.FirstName" class="form-control"></InputText>
    </div>

    <div class="form-group">
        <label>Last Name</label>
        <InputText @bind-Value="Hero.LastName" class="form-control"></InputText>
    </div>

    <div class="form-group">
        <label>Hero Name</label>
        <InputText @bind-Value="Hero.HeroName" class="form-control"></InputText>
    </div>

    <div class="form-group">
        <label>Comic</label> <br />
        @foreach (var comic in SuperHeroService.Comics)
        {
            <input type="radio" value="@comic.Id" name="group" checked="@ComicId"  @onclick="@(()=>ComicId = comic.Id)" /> @comic.Name
        }
    </div>
           @*<InputSelect @bind-Value="ComicId" class="form-control">
            @foreach (var comic in SuperHeroService.Comics)
            {
            <option Value="@comic.Id">@comic.Name</option>
            }
        </InputSelect>

        <CustomInputSelect id="department" @bind-Value="ComicId" class="form-control">
            @foreach (var comic in SuperHeroService.Comics)
            {
            <option value="@comic.Id">@comic.Name</option>
            }
        </CustomInputSelect>
        <InputRadioGroup @bind-Value="" class="form-control">
            @foreach(var comic in SuperHeroService.Comics)
            {
            <inputRadio Value="@comic.Id" /><span>@comic.Name</span> <br />
            }

        </InputRadioGroup>
        

        <button type="submit" class="btn btn-primary">Submit</button>

    </EditForm>*@
@code {

}
